{
  "name": "diary-app",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "lint": "biome lint --write .",
    "format": "biome format --write .",
    "check": "biome check --write ."
  },
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "vitest": "^2.0.5"
  }
}
